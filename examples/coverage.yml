name: Upload code coverage to Codecov

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  coverage:
    uses: rayxiom/workflows/.github/workflows/coverage.yml@v1
    needs: conventional-ci
    permissions:
      id-token: write
